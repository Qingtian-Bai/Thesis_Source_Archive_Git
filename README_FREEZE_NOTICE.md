# Thesis Technical Freeze: 6FE329 (18 August 2026)

This directory preserves the code, launcher, configuration, two custom weights, YOLO pose and COCO weights, input recording, complete regression logs, and retained evidence used for the thesis delivery.

- Door v5 provides authoritative mobile-phone alerts.
- The later custom weights provide paper and note context only.
- The 6FE329 result on the existing recording is labelled as a post-test engineering regression, not as a new independent test.
- `FROZEN_MANIFEST.json` records the key conclusions and file-level SHA-256 values.
- `SHA256SUMS.txt` supports byte-for-byte integrity verification before submission.

The release files are read-only. Copy the package to a new directory before carrying out any further development; do not modify this submitted technical freeze.
